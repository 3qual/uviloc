import CustomText from "../CustomText";

const Map = () => {
    return (
        <CustomText style={{backgroundColor: "black"}}>
            Map is not available on web.
        </CustomText>
    );
}

export default Map;